CH	EQU	$24
HOME	EQU	$FC58
COUT	EQU	$FDED
TABV	EQU	$FB5B

	ORG	$8000
	JSR	HOME	; VIDER L'ECRAN
	LDA	#30	; COLONNE 30
	STA	CH
	LDA	#10	; LIGNE 10
	JSR	TABV
	LDX	#0
PRINT	LDA	MSG,X	; PRINT UN SEUL CHARACTERE
	BEQ	FINI	; 0 => FINI
	JSR	COUT	; AFFICHER CHAR
	INX		; PROCHAIN CHAR
	BNE	PRINT	; TOUJOURS VRAI
FINI	RTS

MSG	ASC	"BONJOUR"
